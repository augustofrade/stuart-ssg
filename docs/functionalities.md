# Core functionality

- Create projects with or without a blueprint
- Set a theme to a project
- Create a page with YAML frontmatter and markdownn, with or without a page model
- Parse page YAML frontmatter
- Inject frontmatter and project conf values into pages
- Render page markdown
